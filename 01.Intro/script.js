// Types
// Primitives
let a = 42; // number
let b = `Hello, World!`; // string
let c = true; // boolean
let d = null; // null
let e; // undefined

// Non-primitives
let f = [1, 2, 3]; // array
let g = { name: "Alice", age: 30 }; // object

// Functions
function add(x, y) {
    return x + y;
}

add(5, 10);